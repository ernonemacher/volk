.DS_Store

# Each submodule carries its own ignores; nothing is built at this level.

# Scratch from tooling that runs inside the repo, not part of the project.
.playwright-mcp/
.agents/
.claude/
skills-lock.json
